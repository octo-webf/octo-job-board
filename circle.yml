machine:
  node:
   version: 7

test:
  post:
    - (cd serfver && npm run coverage)

deployment:
  staging:
    branch: dev
    heroku:
      appname: jobs-octo-test
  production:
    branch: master
    heroku:
      appname: jobs-octo
